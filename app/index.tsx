import { SafeAreaView } from "react-native-safe-area-context";

const Home = () => {
    return (
        <SafeAreaView>
        <h1>Home</h1>
        <p>Welcome to the home page</p>
        </SafeAreaView>
    );
}

export default Home;